name: CopyRepo

on:
  workflow_dispatch:
    

jobs:
  Copy-Repo-10-1-1-2:

    runs-on: ubuntu2
    steps:
      #- name: Checkout Source Repository
      #  uses: actions/checkout@v4
      #  with:
      #    repository: dxmohan/project4.git # Replace with your source repo
      #    ref: releases/101010 # The branch to checkout
      #    token: ${{ secrets.PAT_TOKEN1 }} # Use the PAT for necessary permissions

      

     

      - name: Push to Destination Repository
        run: |
          #export HOME=/root
          git config --global user.name "Mohandass"
          git config --global user.email "dxmohan@gmail.com"
          
          echo $GITHUB_REF_NAME
          rm -rf project4/
          git config user.name "Mohandass"
          git config user.email "dxmohan@gmail.com"
          git clone  -b $GITHUB_REF_NAME https://${{ secrets.PAT_TOKEN1 }}@github.com/dxmohan/project4.git
          cd project4
          git push https://${{ secrets.PAT_TOKEN1 }}@github.com/dxmohan/project1.git $GITHUB_REF_NAME:$GITHUB_REF_NAME
          cd -
          
       
